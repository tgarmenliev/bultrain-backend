# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is committed so the admin panel deploys with a plain `git pull` (it holds
# no secrets — it's the same client bundle already served publicly at /admin).
# Rebuild it (npm run build) and commit whenever admin-ui/src changes.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
